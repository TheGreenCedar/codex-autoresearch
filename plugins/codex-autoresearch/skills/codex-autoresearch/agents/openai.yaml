interface:
  display_name: "Codex Autoresearch"
  short_description: "Run measured, resumable improvement loops"
  default_prompt: "Use $codex-autoresearch to classify fit before repository discovery. Continue directly with the evidence capsule unless the request explicitly defines a complete repeated measured loop; ask only for exact missing contract fields and leave unrelated sessions untouched."
