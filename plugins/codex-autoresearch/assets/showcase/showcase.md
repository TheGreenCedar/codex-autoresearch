# Dashboard showcase

`dashboard-demo.png` is the synthetic example shown in the public README. It demonstrates the read-only dashboard; its measurements are not product-performance evidence.

The real-browser dashboard check captures the bundled 100-packet example as `tmp/dashboard-operator-demo-details.png` after validating the selected-run dialog and closing it. Inspect that image alongside the 390×844 capture before copying it here. Show readable chart labels, populated data, and the selected value without overlays, personal data, local absolute paths, or browser chrome.
